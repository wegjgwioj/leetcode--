nums[] int
最长 严格递增 子序列  原数组相对位置 和 数值不连续
0 1 0 3 2 3
0 1 2 3

dp[i]:nums[i] 结尾的最长递增子序列的长度==> i也可以作为nums索引 
     dp[i] = max(dp[i], dp[j] + 1)
dp[0]=0
dp[1]=1

dp[2]=1 or 2

```python
import math
 if __name__ == "__main__":
    nums=[0 ,1, 0 ,3 ,2, 3]
    l=len(nums)
    dp=[1]*l

    dp[0]=0
    dp[1]=1

    for i in range(2,len+1):
        for j in range(i):
            if nums[i]>nums[j]:
                dp[i] = max(dp[i],dp[j]+1)
    print(max(dp))

```
