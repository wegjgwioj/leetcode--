string s
stringlist wordDict 


```python
  
   import math

  # dp[i] :代表 s[0:i]能被 wordDict 中的单词拆分成一个或多个单词
 

if __name__ == "__main__":
    worDict=['leet','code']
    s="leetcod"
    l=len(s)
    myset=set(worDict)#转set
    dp=[False]*(l+1)
    dp[0]=True
 
    for i in range(1,l+1):
        for j in range(i):
            if dp[j] and s[j:i] in myset: # 往前看
                dp[i] = True 
                break
    print(dp[l])
            


```

# set
在列表（List）中查找元素需要从头到尾遍历一遍；而在集合（Set）中查找元素的时间是固定的（利用哈希表），速度极快